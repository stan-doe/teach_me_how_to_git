let a = 1;
let b = 2;

function sum(a, b) {
    return a+b;
}
function divide(a, b) {
    return a/b;
}

console.log(`Sum ${a} + ${b} equal`, sum(a,b));
console.log(`Divide ${a} / ${b} equal`, divide(a,b));