let a = 1;
let b = 2;

function sum(a, b) {
    return a+b;
}
function multiply(a, b) {
    return a*b;
}

console.log(`Sum ${a} + ${b} equal`, sum(a,b));
console.log(`Multiply ${a} * ${b} equal`, multiply(a,b));